[package]
name = "virtio"
version = "0.1.0"

[lib]
name = "virtio"
path = "lib.rs"

[dependencies]
bitflags = { version = "0.9" }
intrusive-collections = { version = "0.8.1", default-features = false, features = [ "nightly", "alloc" ]}
kernel = { path = "../../kernel" }
pci = { path = "../pci" }
